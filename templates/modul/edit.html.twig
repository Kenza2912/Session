{% extends "base.html.twig" %}

{% block title %} Modifier un module{% endblock %}

{% block body %}
	<div class="container">

		<h1 class="mt-4">Modification d'un modul</h1>

		{{ form_start(form) }}

		<div class="form-group">
			{{ form_label(form.nameModul) }}
			{{ form_widget(form.nameModul) }}
			<div class="form-error">
				{{ form_errors(form.nameModul) }}
			</div>
		</div>


		<div class="form-group">
			{{ form_row(form._token) }}
		</div>

		<button type="submit"  class="btn btn-primary mt-4">Modifier une recette</button>

	</div>
	{{ form_end(form) }}
</div>{% endblock %}